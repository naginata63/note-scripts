{
  "name": "note-scripts",
  "version": "1.0.0",
  "description": "note.comの記事管理をCLIで自動化するPlaywrightスクリプト",
  "scripts": {
    "install-browsers": "npx playwright install chromium"
  },
  "dependencies": {
    "dotenv": "^17.3.1",
    "playwright": "^1.50.0"
  }
}
